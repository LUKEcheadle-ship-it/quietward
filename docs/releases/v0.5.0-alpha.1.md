# QuietWard v0.5.0-alpha.1

Status: **candidate — detection gate and platform qualification required before publication**

Python package version: `0.5.0a1`

## Purpose

v0.5 strengthens QuietWard's deterministic detection/correlation layer while preserving its public-product contract: offline-first, privacy-conscious, observation-only, localhost-only, and incapable of automatic remediation.

This release contains no QuietWard Response client, agent, action, or integration code.

## Detection improvements

### Same-host multi-stage attack chains

QuietWard can now correlate high-confidence events across different subjects on the same host inside a bounded 15-minute window. This allows a sequence such as authentication attack → privilege escalation → persistence → suspicious outbound connection to become one higher-priority finding rather than several unrelated subject-specific findings.

A cross-subject chain requires multiple attack phases and multiple subjects. Background event diversity alone does not qualify.

### Credential spray

Linux SSH and Windows authentication collectors now retain privacy-preserving source/account context sufficient to recognize one source targeting multiple accounts:

- raw source IP is not persisted;
- raw username is not persisted;
- account identities are installation-scoped pseudonymous hashes;
- source address is stored only as a stable hash;
- large multi-account bursts are promoted to HIGH priority when sufficiently corroborated.

### High-confidence behavioral scoring

Explicit collector markers now receive deterministic bounded weighting. High-confidence behavior can receive a HIGH priority floor for markers including:

- reverse shell;
- credential dumping/theft;
- process injection;
- web shell;
- dangerous container configuration;
- Docker socket/host-root exposure;
- document/PDF process spawning a high-risk interpreter or LOLBin.

The scoring floor affects review priority only. It cannot authorize or execute an action.

### Windows parent→child behavior

A document/PDF application spawning a known high-risk interpreter/LOLBin is marked `document_spawned_interpreter`. The detector uses process names and parent PID already present in the read-only inventory. Raw command lines remain protected by the existing privacy design.

False-positive regressions cover normal Office child processes and interpreters launched from non-document parents.

## Safety boundary

Unchanged. QuietWard does not:

- quarantine/delete files;
- terminate processes/services;
- change firewall rules;
- isolate a host;
- install packages during monitoring;
- upload telemetry;
- expose a public listener;
- execute arbitrary commands;
- perform automatic remediation.

Required invariants remain:

```text
actions_executed == 0
executable_proposals == 0
dashboard_bind == 127.0.0.1
cloud_upload == false
public_listener == false
```

## Qualification

Run:

```text
python scripts/verify_v05_detection.py
```

The candidate must also complete the existing Windows 11 and Debian 12 platform qualification before publication.

Do not publish/tag `v0.5.0-alpha.1` if any detection, privacy, safety, public-release, or platform qualification check fails.
